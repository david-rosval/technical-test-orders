import 'dotenv/config'

const {
  DB_HOST,
  DB_MYSQL_PORT,
  DB_USER,
  DB_PASSWORD,
  DB_DATABASE,
  PORT
} = process.env


export {
  DB_HOST,
  DB_MYSQL_PORT,
  DB_USER,
  DB_PASSWORD,
  DB_DATABASE,
  PORT
}
